export default {
  acceptableMinOperationDelay: 1200,
  // Actions with delay less than this value(ms) will be treated as continuous.
  acceptableMinCursorMoveDelay: 600,
  // Actions with delay less than this value(ms) will be treated as continuous.
};
